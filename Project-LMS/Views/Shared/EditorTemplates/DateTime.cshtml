@model System.DateTime?
<input data-val="true"
       data-val-date="The field New startdate must be a date."
       data-val-required="The New startdate field is required."
       type="text" value="@(Model?.ToString("yyyy-MM-dd") ?? DateTime.Now.ToString("yyyy-MM-dd"))">
